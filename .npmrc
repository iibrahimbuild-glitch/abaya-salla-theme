engine-strict=true
# Don't hard-fail when optional native deps' build scripts are ignored.
# Without this, pnpm 11 exits non-zero on ERR_PNPM_IGNORED_BUILDS, which
# breaks `pnpm run watch` used by `salla theme preview`.
strict-dep-builds=false
